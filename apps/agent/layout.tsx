/**
 * Agent App Layout
 * 
 * This is a reference layout for the agent app.
 * Simplified interface for customer service agents.
 * 
 * To fully separate apps, create agent-specific pages
 * with simplified UI focused on chat operations.
 */

export default function AgentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
